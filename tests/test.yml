# Playbook to configure system for user = malcolm
#
# Example command
# ansible-playbook ansible-role-malcolm/tests/test.yml --ask-become-pass

- hosts: localhost
  connection: local
  become: true
  roles:
   - ansible-role-malcolm
