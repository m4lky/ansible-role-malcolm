# Playbook to run Samba install role
#
# Example command
# ansible-playbook ansible-role-maclcolm/tests/test.yml

- hosts: localhost
  connection: local
  become: true
  roles:
   - ansible-role-malcolm
